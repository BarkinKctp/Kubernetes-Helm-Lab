apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../../base

patchesStrategicMerge:
  - replicas.yaml

configMapGenerator:
  - name: kustom-map-config
    env: config.properties
