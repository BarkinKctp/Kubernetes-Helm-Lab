apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: prod

resources:
  - ../../base

patchesStrategicMerge:
  - replicas.yml

configMapGenerator:
  - name: kustom-map
    env: config.properties
